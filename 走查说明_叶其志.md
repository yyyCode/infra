# 走查说明 · 容器任务执行器排障实战

作者:叶其志

本文按「从初版到生产可用」的顺序,说明每个阶段改了什么、每个坑怎么定位的、
以及已知但未修复的问题。完整修改过程见 git log(每阶段一个提交 / PR)。

---

## 一、起点:初版(仅跑通最顺利情况)

初版 `runner.py` 约 100 行,只做一件事:读任务清单,用一个 for 循环逐条
`docker run` 执行,把输出打到终端,按退出码记成败。它能跑通短命令、快速成功的
「顺利任务」,但真实批量下问题很多——这些缺陷是刻意保留的,正是后续要修的坑:

- 串行执行,无并发、无资源限额
- 无超时,一个 `sleep 600` 卡死整批
- 无状态持久化,进程一挂全部丢失
- 无失败分类、无重试
- 输出被 `capture_output` 吞掉,跑完才打印,没有独立日志
- 退出容器堆积、悬空镜像无人清理

改造原则:小步提交,每个阶段对应 accept.sh 的一条验收项,让它多过一项。

---

## 二、逐阶段走查

### 阶段 0 · 打地基

**改了什么**:引入 SQLite 状态库(`tasks` 表:id/command/state/exit_code/
log_path/attempts/时间戳);容器固定命名 `runner-<id>`;每任务输出落独立
`logs/<id>.log`;把「读清单入库」和「执行」拆成两步。

**为什么先做这个**:后面所有阶段(恢复、重试、查询、清理)都依赖「状态能落库、
容器可寻址、日志可追溯」这三样地基,所以先铺好。

### 阶段 1 · 并发与资源限额

**改了什么**:用 `ThreadPoolExecutor(max_workers=--concurrency)` 真正限制在飞
任务数;每个容器加 `--memory` / `--memory-swap` / `--cpus` 限额;识别 OOM。

**坑怎么定位的**:
- 吃内存任务会拖垮宿主机 → 根因是 `docker run` 没带 `--memory`,容器能无限
  申请内存。加 `--memory` + `--memory-swap`(相等,禁止用 swap 绕过)后,超限
  的容器被 OOM Kill 而非拖垮宿主。
- 怎么识别 OOM:退出码 137(被 SIGKILL)只是线索,再用
  `docker inspect -f '{{.State.OOMKilled}}'` 二次确认,判为 failed。
- 并发引入后 SQLite 报线程错 → 默认连接非线程安全,改
  `check_same_thread=False` 共享连接,并用一把锁 `_db_lock` 串行化所有写。

### 阶段 2 · 超时强杀

**改了什么**:每任务带 `--timeout`;超时后 `docker stop`(宽限 SIGTERM)→
`docker kill`(兜底 SIGKILL);超时任务记为 `timed_out`。

**坑怎么定位的**:最初以为 `subprocess` 的 `timeout` 就能杀掉任务,实测发现
超时后容器还在后台跑、子进程仍在。根因:`subprocess.wait(timeout)` 只杀本地的
`docker run` **客户端**进程,容器是独立进程、不受影响。定位后改用 `Popen` +
`wait(timeout)` 捕获超时,再显式对容器 `docker stop/kill`。容器是独立 PID
namespace,杀掉容器即杀掉里面 PID 1 及派生的整棵子进程树,不留僵尸。

### 阶段 3 · 状态持久化与崩溃恢复

**改了什么**:状态即时落库(queued→running→终态);重启时 `recover_interrupted`
把残留在 `running` 的任务重置为 `queued` 重跑;`enqueue_tasks` 改为按命令去重;
只执行 `queued`,已成功绝不重跑;启动清理遗留容器。新增 `--resume`。

**坑怎么定位的**:
- 初版「库里有任务就整体跳过导入」太粗糙,导致换清单跑时出现「待执行 0 条」——
  因为旧库里的任务不是 queued,新清单又没被导入。改成按命令去重后,重跑同一
  清单只补跑未完成的,已成功的不被重置。
- 崩溃恢复的判据:runner 正常结束不会留下 `running`,所以重启后凡是 `running`
  就代表「上次被 kill -9 打断、没跑完」,据此重置重跑。已 succeeded/failed/
  timed_out 一律不动,靠 DB 终态天然幂等。

### 阶段 4 · 失败分类与重试

**改了什么**:`classify()` 区分四类失败并分策略重试;`run_task` 拆为
`run_once`(单次执行)+ 重试循环;`--max-retries` 限上限,`attempts` 落库。

**坑怎么定位的**:关键是区分「容器没起来」和「任务自己跑挂了」——靠 docker
退出码判断:125/126/127 是 docker 客户端错误(镜像拉不到、入口点不可执行),
属基础设施失败,容器根本没起,值得指数退避重试;其余非零是容器内命令自己的
退出码,属任务失败,短间隔重试。OOM / 超时是确定性失败,重试也是白费,直接
落终态、不浪费重试次数。

### 阶段 5 · 日志与产物 + 按 ID 查询

**改了什么**:每个 `logs/<id>.log` 顶部写入头部(任务 ID / 命令 / 第几次尝试 /
时间),日志文件自解释;新增只读查询 `--status`(全部/单个)和 `--log ID`,
不触发执行、不需要 docker。

**坑怎么定位的**:排障时「一次查多个任务的状态和日志」很麻烦,得手敲 sqlite。
补上查询子命令后,`--status` 一张表看全,`--log ID` 直接看内容。此阶段还借回归
测试抓到一个问题:给 `run_once` 加 `attempt` 参数后,阶段 4 的 mock 签名没跟上、
立刻报错,及时修正——说明自动化测试的回归保护有效。

### 阶段 6 · 资源清理

**改了什么**:`run_once` 用 try/finally,保证容器在成功/失败/超时/异常路径都被
`docker rm`,不堆积;批次结束 `docker image prune -f` 清悬空镜像(`--no-prune`
可关);启动 `cleanup_stale_containers` 兜底崩溃残留。

**机制而非手动**:生命周期是「谁创建谁负责删」——run_once 起容器、finally 删
容器;main 批次结束统一 prune 镜像;启动清理兜底进程崩溃时来不及删的残留。
特意不用 `--rm`,因为超时/OOM 场景需要先 `docker inspect` 读 OOMKilled 才能
正确分类,`--rm` 会在容器退出瞬间抢先删掉、导致读不到状态,所以手动在「判定
之后」删。

---

## 三、验收与测试

- accept.sh 6 项对应上述 6 个阶段;在有 docker 的 Linux 上跑 `bash accept.sh`
  可全部通过。
- 逻辑层面有不依赖 docker 的自动化测试(`tests/` 下,mock 掉 docker 调用):
  分类/重试(test_retry)、查询(test_query)、清理(test_cleanup),共 23 个用例,
  `python3 -m unittest tests.test_retry tests.test_query tests.test_cleanup` 全过。

---

## 四、已知未修复 / 可改进项

1. **真机 OOM 路径需最终确认**:自动化测试用 mock 验证逻辑,真实的「超时是否
   真杀干净子进程、OOM 是否真被 137 捕获、prune 是否真生效」仍需在有 docker 的
   环境跑一遍 accept.sh 确认。tasks.txt 吃内存那条命令原先写成带字面 `\n` 的
   形式(容器里是语法错误、没真吃内存),已单独修正为 `iter(int,1)` 无限分配。
2. **SQLite 用一把全局写锁串行化**:并发量不大时够用,但写锁会成为瓶颈;更优
   做法是开 WAL 模式或每线程独立连接。
3. **重试退避策略较简单**:infra 用 2^n 退避、task 用固定 1s;未加抖动(jitter),
   也没有区分「可重试的 task 失败」和「注定失败的 task 失败」,后者仍会重试到
   上限。
4. **日志无滚动/上限**:输出量大的任务日志文件会无限增长,未做大小限制或轮转。
5. **镜像 prune 是全局的**:`docker image prune -f` 会清掉本机所有悬空镜像,
   在多用途机器上可能误伤;更稳妥是只清本次拉取产生的。
6. **无优雅停机**:runner 收到 Ctrl-C / SIGTERM 时未主动清理在飞容器,依赖下次
   启动的兜底清理;可加信号处理器做即时清理。

