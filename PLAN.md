# 改造计划书 · 容器任务执行器

当前状态：`runner.py` 只是「半成品」，只能跑通最顺利的情况(短命令、快速成功)。
`accept.sh` 的 6 大验收项对着这个初版**全部不通过**——这是练习的起点。

本计划把改造拆成 6 个阶段，每个阶段对应一条验收项。**先只做计划，不动代码**，
后续你逐条提问时再按阶段实施。原则：小步提交、每阶段结束能让 `accept.sh` 多过一项。

---

## 阶段 0 · 基线与脚手架(准备工作)

在动任何功能前，先把「能观测、能验收」的地基打好。

- 引入 SQLite：建表 `tasks(id, command, state, exit_code, log_path, attempts, started_at, ended_at)`，
  `state ∈ {queued, running, succeeded, failed, timed_out}`。
- 给每个容器固定命名 `runner-<id>`，方便 `docker inspect` / 清理。
- 建 `logs/` 目录，约定每任务一份 `logs/<id>.log`。
- 把「读清单 → 入库为 queued」和「执行」拆成两步，为后面的并发/恢复铺路。

产出：DB 文件出现、日志目录出现。`accept.sh` 的第 3、5 项开始有条件通过。

---

## 阶段 1 · 并发与资源限额(验收项 1)

- 并发数可配置(如 `--concurrency N` 或环境变量)，用线程池 / 信号量真正限制在飞任务数。
- 每个 `docker run` 带上 `--memory`、`--memory-swap`、`--cpus` 限额。
- 内存超限：容器被 OOM Kill 后，用 `docker inspect` 读 `State.OOMKilled` / 退出码 137
  识别为**失败**而非成功，且宿主机不被拖垮。

产出：吃内存的任务被判定 `failed`，宿主机存活。第 1 项通过。

---

## 阶段 2 · 超时强杀(验收项 2)

- 每个任务带超时(如 `--timeout S`)。超时后 `docker stop`(给宽限)→ `docker kill` 兜底。
- 关键：容器内派生的子进程要随容器一起死。容器本身是 PID namespace，
  杀容器即杀掉整棵进程树；确认不残留僵尸容器(`docker ps -a` 干净)。
- 超时任务状态记为 `timed_out`。

产出：`sleep 600`、多子进程任务被真正杀掉。第 2、2b 项通过。

---

## 阶段 3 · 状态持久化与崩溃恢复(验收项 3)

- 每次状态变更即时写库(queued→running→终态)，保证 `kill -9` 后 DB 反映真实进度。
- 重启时：把残留在 `running` 的任务(上次崩溃时在飞的)重新入队执行；
  已 `succeeded` 的**绝不重跑**(幂等，靠 DB 里的终态判断)。
- 启动时清理上一轮遗留的 `runner-*` 容器,避免重名冲突。

产出：kill -9 → 重启能续跑,成功任务不重复。第 3 项稳定通过。

---

## 阶段 4 · 失败分类与重试(验收项 4)

- 区分两类失败：
  - **基础设施失败**(容器没起来:拉镜像失败、docker 报错) → 可重试,退避重试。
  - **任务自身失败**(命令跑出非零退出码,如 `exit 7`) → 按策略重试,记录真实退出码。
- 重试有上限 `--max-retries`，`attempts` 落库；重跑绝不碰已成功任务。

产出：失败任务退出码被正确记录(如 7),重试受限。第 4 项通过。

---

## 阶段 5 · 日志与产物(验收项 5)

- 把每个任务容器的 stdout/stderr 实时/结束时写入 `logs/<id>.log`。
- 提供查询：按任务 ID 查到 状态 / 退出码 / 日志路径(可加个 `--status` 子命令查库)。

产出：每任务独立日志齐全,可按 ID 追溯。第 5 项通过。

---

## 阶段 6 · 资源清理(验收项 6)

- 机制化而非手动：
  - `docker run --rm` 或结束后显式 `docker rm`,退出容器不堆积。
  - 批次结束(或定期)清理悬空镜像 `docker image prune`。
  - 说清生命周期:谁创建、谁负责删、异常路径也能删(用 try/finally 或启动清理兜底)。

产出：跑完不残留退出容器、无悬空镜像增长。第 6 项通过。

---

## 里程碑与验收对应

| 阶段 | 验收项 | 通过标志 |
|---|---|---|
| 0 | 3、5(部分) | DB / logs 目录出现 |
| 1 | 1 | 内存任务被 OOM 判失败,宿主存活 |
| 2 | 2、2b | 超时任务被杀,无僵尸容器 |
| 3 | 3 | kill -9 后可恢复,成功不重跑 |
| 4 | 4 | 失败退出码正确,重试受限 |
| 5 | 5 | 每任务独立日志可追溯 |
| 6 | 6 | 无退出容器 / 悬空镜像残留 |

> 实施顺序建议 0 → 1 → 2 → 3 → 4 → 5 → 6，因为后面的阶段依赖前面打下的
> DB / 命名 / 日志地基。每阶段单独提交,保留 git log。
