# Infra 实习生实操题 · 2 小时容器任务执行器排障实战

**作答说明**：这是一道**现场实操题，限时 2 小时**。你需要一台能跑 Docker 的机器（自己的笔记本，或任意一台最便宜的 Linux 云主机均可，无需 GPU、无需 Kubernetes）。允许查文档与搜索，不允许让他人代做。我们看重的是"能真实跑起来"和排障过程，不看重代码量与架构名词。

## 场景

我们在大规模生产大模型评测任务：一批任务要分别在独立 Docker 容器里执行，需要并发控制、超时强杀、失败重试、状态持久化和日志留存。团队里有一位同学写了一个任务执行器的半成品（Python，约 200 行），**它能跑通"最顺利的情况"，但在真实批量运行下问题很多**。你的任务：把它修到生产可用的最低标准。

面试开始时你会拿到一个 Git 仓库，内含：

- `runner.py`：任务执行器半成品——读一个任务清单（每行一条 shell 命令），逐个起 Docker 容器执行；

- `tasks.txt`：一组测试任务，其中混有正常任务、运行很久的超时任务、会吃光内存的任务、会派生一堆子进程的任务、以及一条本身就会失败的任务；

- `accept.sh`：验收脚本，跑完后检查各项行为是否正确（当前全部不通过）。

## 目标：2 小时内让 accept\.sh 全部通过

验收项（即 runner 当前已知的缺陷方向，具体坑在代码里）：

1. **并发与限额**：并发数可配置且真的被限制住；容器必须带 CPU / 内存限额，内存超限时容器被 OOM Kill 要能被正确识别，而不是拖垮宿主机；

2. **超时强杀**：任务超时后，整个容器（含任务进程派生的所有子进程）必须被真正杀死，不留僵尸容器；

3. **状态持久化与崩溃恢复**：任务状态（queued / running / succeeded / failed / timed\_out）落 SQLite；runner 进程被 kill \-9 后重启，未完成的任务能恢复执行，已成功的任务绝不重复执行；

4. **失败分类与重试**：区分"容器没起起来"和"任务自己跑挂了"，重试策略分别处理；重试有上限，且重跑不碰已成功任务；

5. **日志与产物**：每个任务的 stdout / stderr 落到独立日志文件，能按任务 ID 查到状态、退出码和日志路径；

6. **资源清理**：跑完一批任务后，机器上不残留已退出的容器、悬空镜像无限增长等垃圾；说出你在机制上怎么保证的，而不只是最后手动清一遍。

## 时间与提交

- 2 小时后提交：你的仓库（git log 保留，我们要看修改过程）\+ 一段 5 分钟的口头走查：你改了什么、每个坑是怎么定位的、还有哪些你知道但没来得及修的问题。

- **做不完是常态**：验收项全部通过是优秀；只通过一部分、但能说清每一项卡在哪、下一步怎么修，同样是好结果。我们评估的是排障思路与工程判断，不是手速。

## 环境要求

- Docker 20\+（本机或一台 2C4G 的 Linux 云机即可），Python 3\.10\+，只装标准库 \+ docker CLI 调用，不依赖任何重型框架；

- 全程不需要 GPU、K8s 或任何付费云服务。

## 提交规范

**提交渠道**：请将完整提交物打包为一个 zip 压缩包，发送至邮箱 **【投递邮箱待确认】**。邮件主题、压缩包与文件夹命名须严格按下方格式执行。

### 命名格式

- **邮件主题**：`Infra实习生笔试题-{你的姓名}-提交`，例如 `Infra实习生笔试题-张三-提交`

- **压缩包 / 顶层文件夹名**：与邮件主题一致

- **走查说明文件名**：`Infra实习生笔试题-走查说明-{你的姓名}.md`

### 提交物结构

压缩包内目录结构如下：

```text
Infra实习生笔试题-{姓名}-提交/
├── runner/                   # 完整 git 仓库（保留 git log），或附 GitHub 链接于 README
│   └── ...
├── 走查说明_{姓名}.md         # 5 分钟走查要点：改了什么、每个坑怎么定位、已知未修复项（或附录屏链接）
└── README.md                 # 仓库链接、运行方式、accept.sh 各项通过情况
```

**git log 必须保留**——我们看重修改过程而非最终代码。如使用 GitHub 仓库，确保面试官可访问。

